export type MoodData = {
  id: string;
  name: string;
};

export type TypeData = {
  id: string;
  name: string;
};

export type DietData = {
  id: string;
  name: string;
};

export type OriginData = {
  id: string;
  name: string;
};
